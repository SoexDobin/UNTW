# 1주차

#### 디지털 신호와 아날로그 신호의 전송
* 디지털 신호 : 2개의 신호(전압, 시간)로 불연속적으로 변함
* 아날로그 신호 : 여러개의 신호로 연속적으로 변함

#### 연결포트
* 다이렉트 케이블
    - 서로 다른 장비를 연결할 때 사용
    - 요즘 시대에서는 다이렉트 케이블을 더 많이 사용
    - ex) PC <-> HUB
* 크로스 케이블
    - 주로 서로 같은 장비를 연결할 때 사용
    - ex) 라우터 <-> 라우터

## 테이터 통신의 목표

#### 소스코딩
* 디지털 형식으로 변환, 압축하는 과정
* 평균 코드 길이가 최소화 되도록 함
* ex) jpeg, bmp, png, zip, lzw
#### 채널코딩
* 전송 데이터에 잉여정보를 추가해 비트 오류율 성능을 개선 시키기 위한 과정
* ex) crc, Hamming code
#### 보안코딩
* 전송 데이터 내용에 안전성을 제공하기 위한 과정
* 대칭키, 비대칭키, 암호 알고리즘

<br>

#### HammingCode
* 오류 검출과 정정을 할 수 있는 코드

#### CRC : 순환중복 검사
* 네트워크 등을 통하여 데이터를 전송할 때 전송된 데이터에 오류가 있는지를 확인하기 위한 체크값을 결정하는 방식
* 송신측에서는 **CRC값을 데이터에 불안 코드워드를 전송하며**, 수신측에서는 수신된 코드워드에서 CRC값을 이용하여 에러 발견

<br>

## 프로토콜
* 이기종 시스템간에도 통신이 가능하게 하기위해 만든 표준 협약
* 정보의 송수신측 또는 네트워크에서 정보를 신뢰성있고 효율적이며 안전하게 주고받기 위해 사전에 약속된 규약, 규범
* 전달되는 정보의 형태, 오류 제어, 동기 방식 등의 약속

### 프로토콜 주요 요소
1. 구문
    - 데이터 형식(Format)
    - 부호화(Coding)
    - 신호레벨(Signal Level)
2. 의미 : 해당 패턴에 대한 해석과 그 해석에 따른 전송제어, 오류수정 등에 관한 제어정보를 규정
3. 타이밍 : 두 객체간의 통신 속도 조정
4. 단편화와 재결합 
    - 단편화 : 응용 계층의 연속적인 비트 스트림, 메시지를 하위 계층에서는 작은 블록으로 나눔
        + 메시지 > 패킷 > 프레임 > 비트 단위로 전송
    - 재결합 : 단편화 된 데이터를 받아 다시 하나로 합치는 기능
        + 재결합은 **목적지에서 이루어진다.(겸)** 
5. 연결 제어
    - 비연결형 데이터 전송 : 데이터를 송수신하는 개체 간에 논리적인 연결 없이 데이터를 전송 ex.) 패킷은 비연결형 : 날 위해 상대 측에서 자원을 할당해주지 않음
    - 연결형 데이터 전송 : 데이터를 송수신하는 개체간에 논리적 연결을 맺은 후 데이터를 전송 
6. 흐름 제어 : **하드웨어의 성능에 따라 흐름제어가 달라진다**
    - 송신측 개체간의 데이터 양이나 속도를 조절하는 기능
    - 송신측과 수신측의 속도차이나 네트워크 내부 문제 등으로 인한 정보 유실 방지
    - [정지 -> 대기] 흐름제어 : 수신측의 확인 신호(ACK)를 받기전에 데이터를 전송하지 않음
    - [슬라이딩 윈도우] 흐름제어 : 확인 신호를 수신하기 전에 데이터의 양을 미리 정해주는 기법
7. 에러 제어
    - 패리티 검사 코드 방식
    - 순환 잉여도 검사  
8. 동기화
    - 두 개체 사이에 정보를 송수신할 떄 초기화 상태, 종료 상태 등의 동기를 맞추는 것
9. 순서화
    - 데이터를 단펴놔하여 전송할 때 데이터들이 올바른 순서로 전송되기 위하여 필요한 기능
10. 프로토콜의 계층화
11. 계층적 독립성
12. 상위 계층 : 사용자가 통신을 쉽게 이용할 수 있도록 도와주는 역할
13. 하위 계층 : 실제 통신의 효율적이고 정확한 전송을 담당하는 역할 
14. SNA(System Network Architecture)
    - IBM이 개발, 컴퓨터 통신망 구조와 체계
    - 대표적인 7계층
    - OSI 기본 참조 모델과 호환성이 없음
15. TCP/IP
    - 미 국방부 개발
    - TCP와 IP를 조합, 4계층으로 구성한 것으로 인터넷에 사용됨
    - RFC (Request For Comment) 형태로 공개
16. OSI(Open Systems Interconnection)
    - 국제 표준화 기구(ISO)에서 제정한 국제적 표준화 망 구조
    - 7계층의 기본 참조 모델을 제정
    - 표준(Standard) : 사회이익의 증진을 목적으로 해서 **과학 기술 및 경험의 종합적 결론이나 이해 관계자의 협력과 모든 의견, 대다수의 승인**에 의해서 작성된 **기술 사양서(technical specification)** 또는 그 외의 문서로 국가, 지역,또는 국제 레벨에서 인정된 단체에 의해 승인된 것
17. 국제 표준기구 :International Standards Organization
    - 1947년 2월에 창설
    - 현재 163개국의 국가표준단체로 구성
    - 전세계의 표준화 및 관련 활동의 개발을 촉진
    - OSI(Open Systems Interconnection) : 다른 기종간의 상호접속을 가능케 하는 표준 개방형 통신망에 대한 제반사항을 규정 네트워크를 위한 7계층의 참조 모델을 정의

# 중간고사 정리

## 프로토콜 

1. **프로토콜 정의**
    * 정보의 송수신측 or 네트워크에서 정보를 신뢰성있고 효율적이며 안전하게 주고받기위한 사전 약속된 규약, 규범
    * 전달되는 정보의 형태, 오류 제어, 동기방식 등의 약속

2. **프로토콜 주요 요소**
    * 구문 : 데이터의 형식(Format), 부호화(Coding), 신호레벨(Signal Levels) 정의   
    ![image](https://github.com/SoexDobin/3DChess/assets/56966606/0e13ef03-430c-4356-add2-1884332ee10e)   
    * 의미 : 해당 패턴에 대한 해석과, 그 해석에 따른 전송제어, 오류수정 등에 관한 제어정보 규정      
    ![image](https://github.com/SoexDobin/3DChess/assets/56966606/779869b7-18b1-490f-9e00-bdd1228fe2fb)   
    * 타이밍 : 두 객체간의 통신 속도 조정   
    ![image](https://github.com/SoexDobin/3DChess/assets/56966606/aa561f4f-dc74-4967-9b80-43fab5f98261)

## OSI 모델

1. OSI : Open System Interconnection
    * 모든 유형의 컴퓨터 시스템 간의 통신을 허용하는 네트워크 시스템의 설계를 위한 계층구조
2. OSI 모델의 목적
    * 히본 하드웨어, 소프트웨어의 변경 없이 다른 시스템간 개방 통신을 위한 것
    * 상호 연동 가능한 유연, 튼튼한 네트워크 구조를 설계하고 이해하기 위한 모델
3. **! 계층구조 장점**
    * 전체 시스템을 이해하기 쉽고, 설계 및 구현이 용이
    * 독립성을 향상 시키며, 시스템 구조를 단순화 시킬 수 있음
    * 특정 모듈의 외부 인터페이스가 변하지 않으면 내부 기능의 변화가 전체 시스템의 동작에 영향X
    * peer to peer process : 해당 계층에서 통신하는 각 장치상 프로세스
    * 계층간 인터페이스 : 상위 계층에서 제공되는 정보의 서비스를 정의

## 계층
1. 1계층 Physical Layer
    - bit 단위
    -  Node-To-Node Delivery
    - 보안성을 확보할 수 있는 매체 - 광섬유(직진성, 혼선X)
2. 2계층 DataLink Layer
    - frame 단위 48bit
    -  Node-To-Node Delivery
    - Mac 물리주소를 가짐
    - 흐름 제어 : 송신노드가 수신노드의 처리속도를 고려하여 이를 초과하지 않도록 전송을 제어
        + Stop and Wait :  송신측에서 프레임을 전송한 후 확인응답(ACK)을 받을 때까지 대기하는 방식
        + 슬라이딩 윈도우 : 송신 츶에서 수신측의 응답을 받기 전까지 가능한 범위내에서 프레임을 전송
    - 오류 제어 :  전송 중에 오류나 손실 발생 시 이를 해결하기 위한 제어 방식
        + FEC(Forward Error Correction): 전진 에러 수정 :  송신측에서 데이터 전송 시 오류를 검출할 수 있는 부가정보를 함께 전송하여 수신측에서 이를 점검하여 오류 발생 시 재전송을 요청하는 방식으로 이러한 요청을 ARQ(Automatic Repeat Request)라 한다.
        + BEC(Backward Error Correction): 후진 에러 수정 : 재전송이 불필요한 방식으로 송신측에서 데이터 송신 시 오류의 검출 및 수정까지 가능한 부가정보를 담아서 보내는 방식
    - 혼잡 제어 : 네트워크내 패킷수가 과다하게 증가하는 혼잡을 방지하고 제거하기 위한 기능
3. 3계층 Network Layer
    - packet 단위 32bit
    - End-To-End Delivery
    - 논리 주소
    - IP 주소와 라우팅 알고리즘
    - TCP, UDP
4. 4계층 Transport Layer
    - segment 단위 16bit
    - Process-To-Process Delivery
5. 5계층 Session Layer
    - message 단위
    - DNS 주소
    - Application간 논리적인 연결인 세션의 생성, 관리 및 종료를 담당하는 계층
6. 6계층 Presentaion Layer
    - message 단위
    - 데이터 표현방식 변환을 담당하는 계층으로 인코딩/디코딩, 압축/압축해제, 암호화/복호화 등을 담당
7. 7계층 Application
    - message 단위
    - 사용자가 네트워크에 접근할 수 있는 인터페이스이며, 네트워크 서버/클라이언트 프로그램이다.

## 흐름제어 & 오류제어 

* For Noisy Channel 
    1. Stop-and-Wait ARQ : ACK를 받고 나서 다음 데이터를 보내는 방식
        + 수신창 전용 윈도우 사이즈은 1임
        * 순서번호를 사용 프레임의 순서를 정함
        + 클럭 1개   
        + ACK
        ![image](https://github.com/SoexDobin/3DChess/assets/56966606/355d9a53-9177-4b7a-954c-6f1241d9ba27)
    2. Go-Back-N ARQ : 연속으로 데이터를 보내다가 오류가 발생한 지점부터 재전송하는 방식
        + 수신창 클럭전용 윈도우 사이즈은 1임
        + 발신창 클럭은 2n-1승 개 만큼
        + 클럭 1개
        * 프레임을 여러 개 보내는 방법
        - 버퍼의 데이터가 순차적이다
        - 성공적으로 전송된 데이터까지 재전송하기 때문에 조금 비효율적   
        + ACK
    
    3. Selective Repeat ARQ : 연속으로 데이터를 보내다가 오류가 발생한 데이터만 재전송하는 방식
        * 프레임을 여러 개 보내는 방법
        + 발신창 클럭은 2n-1승 개 만큼
        - 버퍼의 데이터가 순차적이지 않다
        - 정렬의 과정이 추가로 필요하고 별도의 버퍼가 필요하다   
        + ACK & Nack
        + 클럭 n개
        ![image](https://github.com/SoexDobin/3DChess/assets/56966606/d6ec6677-dbc6-46e7-90ca-93dd90483ec1)

## Lan이더넷의 MAC 프레임

* 2계층인 DataLink 계층에서 사용되는 메커니즘

### MAC header부분
1. Destination 
    + 목적지 MAC(물리)주소를 담고 있음
    + 6byte = 48bit
2. Source Address :
    + 송신자 MAC(물리)주소를 담고 있음
3. Length or Type : 
    + 1518byte 보다 작으면 길이 필드
    + 1536byte 보다 크면 프레임안 캡슐화된 PDU 패킷 타입을 정의
### LLC프레임
1. Data/Payload
    + 상위 계층으로부터 캡슐화된 데이터가 들어있다
    + 데이터 필드는 최소 46byte ~ 1500byte
    + 최소길이가 46보다 작을때 끝에 padding(0)으로 채워진다
### Trailer
1. FCS
    + MacHeader와 LLC프레임의 영역을 계산해 에러를 판별
    + 송신측에서 CRC를 추가해 보내고
    + 수신측은 CRC를 체크하여 버림

## Wan
Wan(World Area Network) : 광역 네트워크(WAN)는 사무실, 데이터 센터, 클라우드 애플리케이션 및 클라우드 스토리지를 서로 연결하는 기술
AAL 계층 : Aplication Adaption Layer은 모든 유형의 네트워크를 ATM설비에 연결할수 있도록 지원
ATM 계층 : 48byte 세그먼트를 받아 5byte 헤더를 추가하고 53byte로 변환
IP 프로토콜은 AAL5 subLayer를 이용한다